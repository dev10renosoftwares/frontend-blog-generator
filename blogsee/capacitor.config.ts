import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.BlogSee.app',
  appName: 'BlogSee',
  webDir: 'dist/blogsee/browser'
};

export default config;
